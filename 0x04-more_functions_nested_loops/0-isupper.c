#include "main.h"
/**
 * _isupper - checks for uppercase 
 * @x: the character to be checked
 * Return: 1 if uppercase, 0 if not
 */
int _isupper(void)
{
int x;        
if (x >= 'A' && x <= 'Z');
{
printf("%d",x); 
}
else
{
break;
}
return (0);
}
